{
  "type": "module",
  "scripts": {
    "build": "mkdir -p output/resume && cp resume.yaml output/ && cp assets/* output/resume/ && node build.js",
    "clean": "rm -rf output",
    "test": "node test.js"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ejs": "^3.1.8",
    "lodash": "^4.17.21",
    "puppeteer": "^19.6.3",
    "word-wrap": "^1.2.3",
    "yaml": "^2.2.1"
  }
}
